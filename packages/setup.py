from setuptools import setup,find_packages

setup(name='winepackage',
      version='0.3',
      description='this is needed for mlops project',
      author='tejpal',
      packages=find_packages(),

      install_requires=[])
